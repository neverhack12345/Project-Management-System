# Implementation map

Source files are **not** duplicated in this skill. Paths below are relative to the **repository root** (parent of `.cursor/`).

| Area | Path |
|------|------|
| Vault path rules, I/O, index, graph cache | `src/vault-store.js` |
| HTTP API (`/api/vault/*`) | `src/server.js` |
| MCP `vault_*` handlers | `src/mcp-server.js` |
| Notes UI page | `public/notes.html` |
| Notes UI client logic | `public/notes.js` |
